apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: portal-stage
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/portal-platform.git
    targetRevision: HEAD
    path: helm/portal
    helm:
      valueFiles:
        - ../../environments/stage/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: portal-stage
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
