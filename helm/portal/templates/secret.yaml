{{- range $name, $rawSvc := .Values.services }}
{{- $svc := include "portal.mergedService" (dict "root" $ "serviceName" $name) | fromYaml }}
{{- if and $svc.enabled $svc.secret.enabled }}
{{- $ctx := dict "root" $ "svc" $svc "serviceName" $name }}
{{- $secretName := $svc.secret.nameOverride | default (include "portal.serviceFullname" (dict "root" $ "serviceName" $name)) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ $.Release.Namespace }}
  labels:
{{ include "portal.labels" $ctx | indent 4 }}
type: {{ $svc.secret.type | default "Opaque" }}
{{- if $svc.secret.data }}
data:
{{ toYaml $svc.secret.data | indent 2 }}
{{- end }}
{{- if $svc.secret.stringData }}
stringData:
{{ toYaml $svc.secret.stringData | indent 2 }}
{{- end }}
{{- end }}
{{- end }}
